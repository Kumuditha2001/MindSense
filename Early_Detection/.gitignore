# Python
venv/
__pycache__/
*.pyc

# Firebase / secrets — never commit these
firebase-service-account.json
*.env

# Old local database (no longer used, but ignore if it reappears)
mindsense.db

# Flutter
mobile_app/.dart_tool/
mobile_app/build/
mobile_app/.flutter-plugins
mobile_app/.flutter-plugins-dependencies

# Trained model files (large — optional to ignore depending on your submission needs)
# models/*.keras
# models/*.save

# OS files
.DS_Store
Thumbs.db
